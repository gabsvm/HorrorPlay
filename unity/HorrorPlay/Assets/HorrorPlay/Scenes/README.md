# Generated scene

`InnsmouthStreet.unity` is created automatically by `StreetProjectSetup` on first Unity import (or via `HorrorPlay > Setup Innsmouth Street V1`). The environment itself is built deterministically by `StreetBootstrap` at runtime.
