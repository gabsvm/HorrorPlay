name: Legacy Regenerate Procedural Inspector Art

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy status
        run: |
          echo "The procedural Inspector generator is retained only for historical/reference purposes."
          echo "Cartoon Detective Pack is the canonical Inspector art source on agent/aa-character-overhaul."
          echo "This workflow intentionally does not write generated art back to the branch."
